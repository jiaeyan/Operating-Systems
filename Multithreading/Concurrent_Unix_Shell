This is an implementation of concurrent Unix shell with multithreading strategy in Java.
Command lines like pwd, cd, ls, wc, uniq, > and exit are implemented, along with background processing function.
